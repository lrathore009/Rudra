# Rudra Backend

Python FastAPI application for the Rudra Personal Intelligence OS.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn rudra.main:app --reload
```
